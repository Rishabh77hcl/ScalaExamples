package concepts.basics.com
/*
import scala.collection.immutable.Stack
import scala.collection.mutable

object test extends App {
  println("Hi Scala")

  val name = "besant"
  println(name)
  val dropped_val = name.drop(2)
  println(dropped_val)
  val result = name==dropped_val

  val result1 = name.filter(_.equals("besant"))
  println(result1)
  val list1:List[Int]=List(1,2,3)
  println(list1)
  name.foreach(println)
  def add1(): Unit =
  {

  }
  //list1.foreach(add1)

  val line = """THis is new line
    |this is sec line,"This ia my query"
  """.stripMargin.replaceAll("/n"," ")
  println(line)
  line
  val splited = name.split("s")
  println(splited(1))
  //name.map(add1())
  val tdate = "25/45/45".toString
  val sql_query = s"select * from tablename where date = $tdate and day =$day /n gff "
  val sql_query = f"select * from tablename where date and day =$day.02F"
  val sql_query = raw"select * from tablename where date = $tdate and day =$day /n gfff"
  val values = Map(1 -> "Delhi", 2 -> "Bangalore", 3 -> "Pune")

  //scala.collection
  values.contains(1)
  values.valuesIterator.exists(_.contains("une"))
  values.retain
  values.filter(_)
  values.filterKeys(_)
  def only1(i: Int) = if (i == 1) true else false
  values.filterKeys(only1(2))
  values.filterKeys(Set(values))
  values.filter(t=>t._1>2)
  values.keys.max
  values.max
  values.reduceLeft()
//adding in set
  var set = scala.collection.mutable.Set[Int]()
  set +=2
  val set2=(set ++= set)
  set.add(6)
  val newset = set++set2
  val newset = set+2
  val newset = set+(1,2,3)
  //deleting in set
  set -=1
  set2-=(2,3)
  set.retain(_>3)
  set.clear()
  set.remove(2)
  val s = scala.collection.SortedSet(10, 4, 8, 2)
  val q = mutable.Queue(1,2,3) //- adding removing is same like set
  q.enqueue(1) //FIFO
  q.dequeue()
  q.dequeueAll()
    //q.dequeueFirst()
    var stack1= Stack("a","b","c")
  stack1.push()
  stack1.pop
  stack1.top
  stack1.size
  stack1.isEmpty
  stack1.clear()

  val r = 1 to 20
  val r1 = 1 until 20/2
  val r3 = (1 to 20).toList
  val r4 = 'a' to 'g'
  println(r4)
  val r = Array.range(1,6)
  val r = List.range(1,6)
  val table_val = (1 to 5).map { ops => (ops + 1) * 2 }
  val x = List.tabulate(5)(_ + 1)
  val lengths = names.map(_.length)
  val elems = nieces.map(niece => <li>{niece}</li>)
  println(if (i == 0) "a" else "b")



*/



  // val pattern = [0-9].r
  //val pattern = ["https"].r
  //name.replaceFirst(pattern,"NA")



